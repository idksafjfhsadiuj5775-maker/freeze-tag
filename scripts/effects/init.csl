import "core:ao"

spawn_winner_trophies :: proc(winner_label: string) {
    announce("Winner trophies spawned: %", {winner_label});
}

clear_winner_trophies :: proc() {
}

apply_cinematic_slow_motion :: proc(enabled: bool) {
}
